rootProject.name = "KotlinChess"

pluginManagement {
    plugins {
        id("org.gradle.toolchains.foojay-resolver-convention") version("0.7.0")
    }
}

plugins {
    id("org.gradle.toolchains.foojay-resolver-convention")
}